export const environment = {
  production: true,
  useBackend: true, // En producción usar el backend real
  apiUrl: 'http://localhost:3000', // URL del JSON Server (para desarrollo local)
  backendUrl: 'https://your-production-backend.com/api', // URL del backend en producción
  appName: 'VitaMeet',
  version: '1.0.0'
};
